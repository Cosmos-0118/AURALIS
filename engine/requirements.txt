# Auralis Python engine — install from project root:
#   python3 -m venv engine/.venv
#   source engine/.venv/bin/activate && pip install -r engine/requirements.txt

# PyTorch (MPS acceleration on Apple Silicon)
torch>=2.1.0
torchaudio>=2.1.0
torchcodec>=0.14.0

# Stem separation, analysis, and DSP
demucs>=4.0.1
librosa>=0.10.2
pedalboard>=0.9.13
soundfile>=0.12.1
numpy>=1.26.0
scipy>=1.11.0

# Optional: LLM parameter generation
# openai>=1.0.0   # OpenRouter / GroqCloud use OpenAI-compatible API
